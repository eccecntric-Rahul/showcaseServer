let mongoose;_fd7‍.x([["default",()=>_fd7‍.o]]);_fd7‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
const {Schema}= mongoose;

const reviewSchema= new Schema({
    guestName: {
        type: String,
        required: "name is required",
        trim: true,
        
    },
    comment: {
        type: String,
        trim: true,
    }, 
    email:{
        type:String,
        trim:true,
        lowercase: true,
        unique:'This email already exists',
    },
    like:{
        type:Boolean,
        default:true
    }   
});


_fd7‍.d(mongoose.model("review",reviewSchema));